import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.codegenstudio',
  appName: 'codegenstudio',
  webDir: 'dist'
};

export default config;
